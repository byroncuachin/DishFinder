<% layout('/layouts/boilerplate') %>
<link rel="stylesheet" href="/stylesheets/ingredients.css">

<h1>Baking & Grains</h1>
<a href="/addIngredients" class="lead">Back</a>

<form action="/addIngredients/grains" method="POST">
    <div class="mt-3">
        <!-- Creating checkboxes for grains -->
        <% grainsArr.forEach(function(grains, i){ %>
        <div class="mt-2 d-inline-flex">
            <% if(inputGrainsArr.indexOf(grains) === -1){ %>
            <input type="checkbox" class="form-control btn-check" id="btn-check-outlined<%= i %>" autocomplete="off"
                name="ingredients" value="<%= grains %>">
            <%} else {%>
            <input type="checkbox" class="form-control btn-check" id="btn-check-outlined<%= i %>" checked
                autocomplete="off" name="ingredients" value="<%= grains %>">
            <% } %>
            <label class="btn btn-outline-dark" for="btn-check-outlined<%= i %>"><%= grains %></label>
        </div>
        <% }); %>
    </div>

    <div class="mt-5">
        <button class="btn btn-dark btn-lg text-white">Submit</button>
    </div>
</form>
</div>